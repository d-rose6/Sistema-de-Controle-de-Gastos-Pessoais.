# 💰 Sistema de Controle de Gastos Pessoais

Este é um projeto desenvolvido em Python para gerenciar finanças de forma simples via terminal, com armazenamento de dados em formato JSON.

## 🛠️ Tecnologias e Conceitos Utilizados
- **Python 3**: Linguagem base.
- **JSON**: Para persistência de dados (os dados não somem ao fechar o programa).
- **Modularização**: Separação de funções em arquivos diferentes para melhor organização.
- **Tratamento de Exceções**: Uso de `try/except` para lidar com erros de entrada e de arquivos.

## 🚀 Como executar
1. Certifique-se de ter o Python instalado.
2. Baixe os arquivos `main.py` e a pasta `funcoes`.
3. Execute o comando:
   ```bash
   python main.py
